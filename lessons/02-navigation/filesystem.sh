#!/bin/bash
# Show the current directory path
echo "Current directory: $(pwd)"

# List files and folders with details
ls -l

# List files in a tree strcuture with 3 levels(you can vary the number of levels in the tree)
ls -L 3
